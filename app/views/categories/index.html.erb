<%= link_to "Back to Articles", articles_path %> <br>
<%= link_to "New Category", new_category_path %>
<style media="screen">
  #cat {
    -moz-border-radius: 15px;
    border-radius: 15px;
    border:solid 3px #feffa7;
    padding:5px;
    background-color: #feffa7;
    width: 50%;
    text-align: center;
    font-family: "Arial", sans-serif;
    color: #636363;
  }
  #cats_list {
    column-count: 3;
  }
  #title {
    	color: #a8a7ff;
  }
</style>

<div id="title">
  <h1> Categories: </h1>
</div>

<div class="wrapper">
  <% @categories.each_slice(3) do |cat1, cat2, cat3| %>
  #TO DO!!!
  <% end %>

</div>

<div id="cats_list">
  <% @categories.each do |category| %>
    <div id="cat">
      <h3><%= category.name %></h3>
    </div>
    <br>
    <% end %>
</div>
